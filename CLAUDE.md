# نظام إدارة معمل ماء RO — دليل المشروع الكامل

> هذا الملف يقرؤه Claude Code تلقائيًا. اقرأه كاملًا قبل أي تعديل —
> فيه كل القرارات والتفاصيل المتفق عليها مع صاحب المشروع.

## ما هو المشروع؟

نظام إدارة **لمعمل ماء RO واحد** (محل واحد، في العراق) يعمل على حاسبة المحل
**بصفر تكاليف تشغيل**: لا استضافة، لا سيرفر، لا دفع إلكتروني، لا اشتراكات.
الواجهة عربية RTL بالكامل، والمندوبون يستلمون الطلبات عبر بوتات تيليجرام.

- **صاحب المشروع**: zeco98 (زيد فؤاد) — mail: zeqo.foad@gmail.com
- **مجلد العمل على ويندوز**: `C:\Users\zeqof\Desktop\Ro\Ro`
- **الفرع الرئيسي للتطوير**: `claude/ro-water-management-system-ux8tvv`
- **لغة التخاطب مع صاحب المشروع**: العربية (لهجة عراقية)

## التقنيات (قرار نهائي — لا تقترح تغييرها)

- Python 3 + Flask (لوحة إدارة ويب محلية) + SQLite (ملف `data.db` واحد)
- بوتات تيليجرام بأسلوب **Long Polling** (لهذا لا نحتاج استضافة ولا Webhook)
- pywebview لفتح النظام كنافذة تطبيق سطح مكتب على ويندوز
- بدون JavaScript frameworks، بدون قواعد بيانات خارجية
- رفضنا Power Platform وFlutter وLaravel عمدًا (التكلفة/التعقيد)

## طريقة التشغيل

| الأمر | الوظيفة |
|---|---|
| `تشغيل.bat` (دبل كلك) | تنصيب المكتبات أول مرة + فتح التطبيق بنافذة خاصة |
| `python desktop.py` | نفس الشيء من سطر الأوامر |
| `python run.py` | وضع الويب فقط: http://localhost:8000 |
| `بناء-exe.bat` | بناء exe مستقل عبر PyInstaller في dist\RoWater |
| `python tests.py` | **الاختبار الشامل (92 فحصًا) — شغّله بعد أي تعديل** |

كلمة مرور اللوحة الافتراضية: `admin` (تُغيَّر من الإعدادات، مخزنة في settings).

## بنية الملفات

```
run.py            تشغيل وضع الويب (يشغّل البوتات والنسخ الاحتياطي أيضًا)
desktop.py        تشغيل كتطبيق نافذة (pywebview، ومع فشلها يفتح المتصفح)
tests.py          الاختبار الشامل — 92 فحصًا بالعربي، يستعمل قاعدة مؤقتة
app/db.py         المخطط + الترحيلات (_add_column) + دورة الـ30 يومًا + النسخ الاحتياطي
app/bot.py        بوتات تيليجرام: مدير بوتات متعددة + كل الأزرار
app/web.py        كل مسارات Flask (create_app)
app/templates/    القوالب العربية RTL (base, dashboard, customers, tanks,
                  orders, order_new, drivers, late, reports, settings, login...)
app/static/style.css  التنسيق كاملًا (لا مكتبات خارجية)
data.db           قاعدة البيانات (خارج git) — بجانب exe عند التجميد
backups/          نسخ احتياطية تلقائية (خارج git)
```

## قاعدة البيانات (SQLite)

- **customers**: sub_no, name, phone, governorate, area, street, landmark
  (أقرب نقطة دالة), maps_url (رابط Google Maps), notes, sub_start_date,
  sub_status (active/suspended), created_at
- **tanks**: number (فريد), capacity (400/500), status
  (available/with_customer/returned/maintenance/damaged/lost), customer_id,
  delivered_date, last_fill_date, fill_count, maintenance_date, damage_reason
  — الخزان ملك الشركة دائمًا ولا يباع
- **drivers**: name, phone, car, areas, telegram_chat_id, link_code (6 أحرف),
  **bot_token (بوت خاص لكل مندوب)**, pending_loc_order, active
- **orders**: customer_id, tank_id, driver_id, status
  (open/claimed/delivered/canceled/postponed), created_at, claimed_at,
  delivered_at, cancel_reason, notes
- **order_messages**: order_id, chat_id, message_id, driver_id —
  لتحديث رسائل الطلب عند كل المندوبين عند تغيّر حالته
- **settings**: key/value — منها factory_name, cycle_days=30, warn_days=5,
  admin_password, bot_token (البوت العام الاحتياطي), secret_key

الترحيلات: أي عمود جديد يضاف عبر `_add_column()` في `db.init()` حتى تترقى
قواعد البيانات القديمة **بلا فقدان بيانات** — لا تعدل الجداول بطريقة أخرى.

## منطق العمل المتفق عليه (لا تغيّره دون سؤال صاحب المشروع)

### دورة الـ30 يومًا (الاشتراكات)
- لا تُخزَّن «الأيام المتبقية/المتأخرة» — تُحسب لحظيًا في `db.cycle_info()`
  من آخر تعبئة (أو بداية الاشتراك إن لم توجد تعبئة).
- اليوم 31 = «متأخر يوم واحد»، اليوم 45 = «متأخر 15 يومًا».
- remaining ≤ warn_days → «سينتهي قريبًا». suspended → «موقوف» ولا يُحسب تأخيره.

### الطلبات والبوتات
- **لكل مندوب بوت تيليجرام خاص به** (رمزه في بطاقة المندوب) لتجنب التداخل،
  و«بوت عام احتياطي» في الإعدادات لمن لا بوت له. `bot.manager_loop()` يدير
  الجميع ويلتقط أي بوت جديد خلال ~10 ثوانٍ دون إعادة تشغيل.
- عند إنشاء الطلب تختار الإدارة: **كل المندوبين** (بث للجميع) أو
  **مندوب/سيارة محددة** (يصل له وحده).
- **أول من يضغط «استلام الطلب» يفوز** (UPDATE مشروط بـ status='open' —
  هذا يمنع التسابق)، وتتحدّث الرسالة عند البقية باسم من استلمه،
  وتبقى بقية الطلبات ظاهرة عندهم.
- أمر «الطلبات» يعرض حتى 10 طلبات مفتوحة مع منطقة كل طلب (OPEN_LIST_LIMIT).
- أزرار المستلِم: تم التعبئة / تأجيل / إلغاء (4 أسباب: المنزل مغلق، الزبون
  ألغى، لا يوجد خزان، سبب آخر) / **📍 تحديث الموقع** / 🗺 ابدأ الملاحة.
- **تحديث الموقع**: يضغط الزر → يُسجَّل pending_loc_order → تيليجرام يطلب
  موقعه (request_location) → عند الإرسال يتحدّث maps_url للمشترك تلقائيًا
  ويُحدَّث زر الملاحة في رسائل الطلب. «إلغاء» يلغي العملية.
- «تم التعبئة» تحدّث الخزان: last_fill_date=اليوم و fill_count+1
  (في `db.mark_delivered()`).
- ربط المندوب: يرسل رمز الربط (6 أحرف) لبوته. البوت الخاص يقبل رمز صاحبه
  فقط ويرفض الغرباء («هذا البوت مخصص لمندوب آخر»).

### حماية البيانات
- SQLite بوضع **WAL** (ضد انقطاع الكهرباء).
- **نسخة احتياطية تلقائية** في `backups/` عند كل تشغيل وكل 6 ساعات،
  تُحفظ آخر 14 نسخة (`db.backup()` + `db.start_auto_backup()`).
- لا يمكن حذف مشترك له طلبات (قيد FK) — يُوقف اشتراكه بدلًا من الحذف.
- عند البناء exe: `BASE_DIR` بجانب الـ exe وليس مجلد PyInstaller المؤقت.

### الواجهة
- عربية RTL بالكامل، الشريط الجانبي يمين، بدون أي مكتبات خارجية.
- لوحة التحكم وصفحات الطلبات/المتأخرين **تتحدّث تلقائيًا كل 30 ثانية**؛
  صفحات النماذج لا تتحدث (حتى لا يضيع الإدخال).

## قواعد للتعديلات القادمة

1. **شغّل `python tests.py` بعد أي تعديل** وأضف فحوصات للميزات الجديدة.
2. الأعمدة الجديدة عبر `_add_column` فقط — لا `DROP` ولا إعادة إنشاء جداول.
3. كل النصوص الظاهرة للمستخدم والمندوب بالعربية.
4. لا تضف أي خدمة خارجية مدفوعة أو تتطلب استضافة — هذا شرط أساسي.
5. `bot.api(method, token, **params)` — الرمز وسيط إلزامي (بوتات متعددة).
6. الالتزامات على الفرع `claude/ro-water-management-system-ux8tvv` والدفع له.

## أفكار مؤجلة (اقترحناها ولم تُطلب بعد)

- طباعة QR/Barcode لأرقام الخزانات
- تنبيه يومي تلقائي لصاحب المحل على تيليجرام بقائمة المتأخرين
- تطبيق/بوت للزبون نفسه لطلب التعبئة
- تتبع GPS لحظي لسيارات التوزيع، وتحسين المسارات
- إدارة المخزون وقطع الغيار
